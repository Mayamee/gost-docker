# Proxy listen port (baked into gost config at build time)
PROXY_PORT=9997

# Docker image name (used by build-image.sh and docker save archive)
IMAGE_NAME=gost-proxy

# Baked into the image at build time
PROXY_USER=machine
PROXY_PASS=super-strong-password
